# ============================================
# SERENIA PRODUCTION CONFIGURATION
# ============================================
# This is a TEMPLATE. Copy to .env and fill with real values.
# IMPORTANT: This file contains NO secrets (see secrets/ for sensitive data)
#
# Usage:
#   1. cp .env.example .env
#   2. Fill all variables below
#   3. Never commit .env to git

# ============================================
# INFRASTRUCTURE & DOMAIN
# ============================================
SERENIA_DOMAIN=serenia.studio
TRAEFIK_ACME_EMAIL=admin@serenia.studio
SERENIA_ROOT_PATH=/api

# ============================================
# DATABASE
# ============================================
POSTGRES_DB=serenia
POSTGRES_USER=serenia
# POSTGRES_PASSWORD → in secrets/db_password

# ============================================
# BACKEND: AUTHENTICATION & QUOTAS
# ============================================
SERENIA_AUTH_EXPIRATION_TIME=3600
SERENIA_AUTH_MAX_USERS=200
SERENIA_AUTH_JWT_ISSUER=serenia
SERENIA_TOKENS_INPUT_LIMIT_DEFAULT=100000
SERENIA_TOKENS_OUTPUT_LIMIT_DEFAULT=100000
SERENIA_TOKENS_TOTAL_LIMIT_DEFAULT=200000

# ============================================
# BACKEND: AI & PAYMENTS
# ============================================
OPENAI_MODEL=gpt-5-mini
# OPENAI_API_KEY → in secrets/openai_api_key
# STRIPE_SECRET_KEY → in secrets/stripe_secret_key
# STRIPE_WEBHOOK_SECRET → in secrets/stripe_webhook_secret

# ============================================
# BACKEND: APPLICATION
# ============================================
SERENIA_SYSTEM_PROMPT=Tu es Serenia.

# ============================================
# BACKEND: EMAIL (SMTP)
# ============================================
QUARKUS_MAILER_HOST=smtp-relay.brevo.com
QUARKUS_MAILER_PORT=587
QUARKUS_MAILER_FROM=noreply@serenia.studio
QUARKUS_MAILER_USERNAME=9d8709001@smtp-brevo.com
QUARKUS_MAILER_START_TLS=REQUIRED
# QUARKUS_MAILER_PASSWORD → in secrets/smtp_password

